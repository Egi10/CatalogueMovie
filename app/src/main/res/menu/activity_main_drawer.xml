<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_house_black_building_shape"
            android:title="@string/home" />
        <item
            android:id="@+id/nav_nowPlaying"
            android:icon="@drawable/ic_movie_clapper_open"
            android:title="@string/now_playing" />
        <item
            android:id="@+id/nav_upComing"
            android:icon="@drawable/ic_play"
            android:title="@string/up_coming" />
    </group>

    <item android:title="@string/setting">
        <menu>
            <item
                android:id="@+id/nav_setting"
                android:icon="@drawable/ic_settings"
                android:title="@string/setting" />
        </menu>
    </item>

</menu>
